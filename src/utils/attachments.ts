const DB_NAME='pocket-notes';const DB_VERSION=2;const STORE='attachments';
interface StoredAttachment{id:string;noteId:string;blob:Blob}
function openDb(){return new Promise<IDBDatabase>((resolve,reject)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('notes'))r.result.createObjectStore('notes',{keyPath:'id'});if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:'id'})};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error??new Error('attachment-db-failed'))})}
export async function saveAttachment(noteId:string,file:File){const id=crypto.randomUUID(),meta={id,name:file.name,type:file.type||'application/octet-stream',size:file.size,createdAt:new Date().toISOString()};const db=await openDb();await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put({id,noteId,blob:file} satisfies StoredAttachment);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});return meta}
export async function getAttachment(id:string){const db=await openDb();return new Promise<Blob|null>((resolve,reject)=>{const r=db.transaction(STORE,'readonly').objectStore(STORE).get(id);r.onsuccess=()=>resolve((r.result as StoredAttachment|undefined)?.blob??null);r.onerror=()=>reject(r.error)})}
export async function deleteAttachment(id:string){const db=await openDb();await new Promise<void>((resolve,reject)=>{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).delete(id);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)})}
